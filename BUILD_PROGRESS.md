# BUILD_PROGRESS.md — per-phase log

## Audit corrections — 2026-07-18

Factual corrections to earlier claims in this log (history below is not rewritten):

- **Phase 14 "Security hardening — complete"** shipped exactly 3 hardening tests (`SecurityHardeningTests.swift`: relay forbidden-field rejection, revocation persistence, path boundary checks). The SECURITY.md "verified" status rested on those three tests plus prior-phase suites, not a broad hardening pass.
- **Phase 12 "Accessibility modes — pass at architecture level"** was 2 accessibility labels (`RiskBadgeView` icon + label) plus system-provided Dynamic Type; no accessibility audit was performed.
- **Phase 9 iPad "side-by-side diff browser"** is a file list + diff detail split in regular width (drill-in on compact) — list+detail navigation, not a true side-by-side diff rendering.
- **Phase 6 "Full user journey with structured events — pass"** ran against the `Fixtures/test-codex-app-server` fixture (demo data). The real Codex binary is not installed (NEEDS-HUMAN #3); no live-provider journey has run.
- **Phase 3 "phrase confirmed on both devices"** was Mac-only: loopback integration tests hosted both the server and client engine on macOS. No physical iPhone has confirmed a phrase (NEEDS-HUMAN #10).
- **Phase 8 "Critical actions cannot be approved from a notification — pass"** passed vacuously: no notification approval surface existed (inert actions), so there was nothing to attack.

### Resolved in this fix wave (2026-07-18)

Verified against the working tree on 2026-07-18 (evening pass): build and test contracts green (`./scripts/build.sh` 5 legs zero warnings; `./scripts/test.sh` 251 Shared + Companion-hosted + 9 Relay tests).

**Wave 1 (relay / release plumbing)**

- [x] Relay hardened: 64 KiB body cap (413), replay cache rejecting replays until request `expiration` (409), per-source-IP rate limit (429 + Retry-After), fail-fast startup without `RELAY_SIGNING_PUBLIC_KEY` (random-key fallback removed), loopback default bind, ring-buffered simulated outbox (500) with a clear simulated-delivery startup log; tests in `Relay/Tests/RelayTests/RelayHardeningTests.swift`.
- [x] `Relay/docker-compose.yml`: signing key required (no empty default), `/healthz` healthcheck, TLS-termination requirement documented.
- [x] Relay Ed25519 private key moved from UserDefaults plaintext to Keychain (`kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`) with one-time migration; relay dispatch failures logged via OSLog + DiagnosticsRecorder. (Residual: no rotation API — key ceremony remains NEEDS-HUMAN.)
- [x] Sparkle reads `SUFeedURL` from Info.plist (env override for dev), https enforced, feed passed via `SPUUpdaterDelegate`, current/previous version recorded; `SUPublicEDKey` remains NEEDS-HUMAN (a real EdDSA signing key must be generated by a human — none was invented).
- [x] Companion relay URL is a configurable setting (default nil → relay disabled); the hardcoded `http://127.0.0.1:8787` force unwrap is removed.
- [x] SettingsView scaffold text removed ("Pairing cryptography arrives in Phase 3", "Configured in Phase 10"); panes state real behavior.
- [x] `scripts/test.sh` pkill narrowed to the exact xctest binary path; `scripts/build.sh` simulator destination parameterized (`SIMULATOR_DEST`); `scripts/ci.sh` added as the one-shot CI entry point. (Shared-for-iOS proof reworked in the evening pass — see Continuation below.)
- [x] `install.sh` precedence bugs fixed, `~/.agentdeck` created before writing `AGENTS.md`, backup accumulation capped at 5.
- [x] `project.yml` reconciled with the committed pbxproj (WidgetExtension target, SwiftTerm 1.13.0 / Sparkle 2.9.4 exact pins, xcodeVersion 16.0, ENABLE_HARDENED_RUNTIME YES for Companion, project-default Swift 6).
- [x] Docs corrected: DEPENDENCIES.md Sparkle status, SECURITY.md T15 wording, DECISIONS.md duplicate ADR number fixed + relay-hosting ADR, new AGENT_CAPABILITY_MATRIX.md and THREAT_MODEL.md, docs/PRIVACY.md expanded to the real data flow.

**Security gates (audit §3)**

- [x] iOS pairing requires a real phrase confirmation: `UIConfirmationDelegate` awaits a user continuation, sheet in `MainTabView.swift`; reconnect fails closed on fingerprint mismatch (`PinnedFingerprintPairingDelegate`). Auto-approve removed.
- [x] Device authentication fails closed when biometrics/passcode are unavailable (test-only seam).
- [x] Approval resolver records the policy decision before consuming the idempotency slot (`AgentSessionOrchestrator`); TTL enforced at resolve time.
- [x] Bounded output buffers: `BoundedLineBuffer` in Codex/ACP/Claude/Grok adapters; capped per-session persisted events with drop + diagnostic event; PTY backpressure (`BoundedLineBufferTests`).
- [x] Demo data `#if DEBUG`-gated (approval seed, demo buttons, demo timeline) — no fixture content in release paths.
- [x] Connection rate limiter keyed on the remote endpoint, checked before TLS-expensive work.
- [x] Pairing `FailureRateLimiter`: 5 failed attempts / 10 min / source, exponential backoff to 300 s, bounded maps, Mac reset path (`RateLimiterTests`).
- [x] Environment-variable allowlist for spawned agents (`AgentEnvironment`) applied across all adapters and the PTY supervisor.
- [x] Push-token writes keyed to the authenticated `peerID`; payload-asserted device IDs never trusted.
- [x] Approval requests expire (`expiresAt` + default TTL; resolver `sweepExpired`; expiry enforced at resolve). Residual: sweeping is lazy (at resolve) — no timer-driven sweep caller yet.
- [x] Notifications use fixed templates (IDs/category/risk only); widget free text redacted + length-capped.
- [x] `pairing.confirm` signature verified server-side; duplicate-sequence frames dropped (`PeerConnectionTests`).
- [x] Executable integrity: digest/team fingerprint at discovery, launch-time re-verification refuses tampered binaries in `CompanionSessionService.registerAdapters` (`ExecutableIntegrityTests`).
- [x] Process-group termination: TERM→KILL escalation targets the whole group (`ProcessTerminationTests`).
- [x] JSON-RPC calls carry deadlines; pending continuations fail on stop; line buffers lock-guarded (`JSONRPCTimeoutTests`).
- [x] iOS reconnect: exponential backoff + jitter + circuit breaker with manual retry.
- [x] Per-line fire-and-forget tasks serialized per session (single AsyncStream read loops; PTY turn-event stream).
- [x] `TerminalSessionModel` MainActor-isolated with 4 MiB scrollback cap and coalesced flush.
- [x] `aps-environment` added to `App/App.entitlements`.

**Product surface (audit §7)**

- [x] iOS core loop: composer → `session.prompt`; New Session → `session.start` (production caller `PairingEngine.handleSessionStart`); live mirrored timeline and synced agent/project inventory (`StateSyncFrames`, `StateSyncMirror`); resume pagination.
- [x] Diff serving: `GitDiffMirror` answers `diff.content` (positional git args, temp-file stdout, 15 s timeout, byte cap).
- [x] iOS attachment transfer coordinator (`AttachmentTransferCoordinator`). Residual: `purgeExpiredAttachments` still has no scheduled caller.
- [x] `DeckTheme` design tokens applied across the app per `docs/DESIGN.md`.

### Continuation (2026-07-18 evening)

- `scripts/build.sh` Shared-for-iOS leg: the log-grep proof was flaky — incremental no-op builds print no `Debug-iphoneos` lines, so a green tree failed the check. The leg now wipes its products directory first and requires `Products/Debug-iphoneos/Shared.swiftmodule/arm64-apple-ios.swiftmodule` to exist afterwards (artifact proof instead of log scraping).
- WidgetExtension: added `INFOPLIST_KEY_NSExtensionPointIdentifier = com.apple.widgetkit-extension` (Debug + Release). Without it the generated appex Info.plist had no `NSExtension` dictionary and installation failed with "Invalid placeholder attributes" on every simulator/device. `project.yml` aligned to the generated-Info.plist approach (its `WidgetExtension/Info.plist` reference pointed at a file that does not exist).
- Doc drift corrected: `SECURITY.md` T15 and `THREAT_MODEL.md` T15/F-009 now record launch-time executable re-verification as implemented.
- Smoke test: app installs and launches on the iPhone 17 simulator (iOS 26.5); Home renders the live-sync empty state ("Nothing Synced Yet") with demo sessions present only because the build is DEBUG.

---

## Phase 15 completion — live connections, pairing UI, widget, Sparkle — 2026-07-18

- **Status:** complete (architecture); distribution credentials still NEEDS-HUMAN

### Implemented
- `IOSRemoteConnectionService.swift` — persistent post-pair `PeerConnection`, `session.event` mirroring, `approval.resolve` forwarding, reconnect.
- `SessionEventMirror.swift` — iOS-side event/approval/session persistence from Mac broadcasts.
- `PairingWindowView.swift` — Companion QR window with countdown, copy payload, refresh.
- `PairedDevicesPane` + menu **Pair New Device** wired to `PairingServerEngine.makeOffer()`.
- `WidgetExtension/` — WidgetKit status widget reading `WidgetSummaryState` via App Group.
- `SparkleUpdateController.swift` — Sparkle 2.9.4 (Companion); manual check + auto when `SPARKLE_FEED_URL` set.
- Schema v6: `devices.last_known_endpoint` for iOS reconnect.
- App Group entitlements (`group.com.agentdeck.shared`) on App + WidgetExtension targets.

### Test evidence
```
$ ./scripts/build.sh && ./scripts/test.sh
==> build.sh OK … zero warnings
==> test.sh OK (181 tests, zero warnings)
```

### Acceptance criteria
- iOS keeps live connection after pair — **pass** (connection adopted, not closed).
- Companion pairing without Terminal — **pass** (QR window + Settings pane).
- Widget performs no session supervision — **pass** (summary-only App Group read).
- Sparkle pipeline — **pass at architecture level** (SPM integrated; signed appcast E2E blocked on NEEDS-HUMAN #2, #6).

### Needs-human (unchanged)
- Apple Developer ID, APNs `.p8`, notarization, production relay/hosting, physical LAN GUI pass (#2, #6, #7, #10).

---

## Phase 15 — Distribution and onboarding — 2026-07-18

- **Status:** partial (pipeline artifacts; credentials NEEDS-HUMAN)

### Implemented
- `scripts/package-dmg.sh` — builds `dist/AgentDeck-Companion.dmg` from Release companion.
- `scripts/notarize.sh` — notarytool submit + stapler (requires NOTARY_* env vars).
- `docs/PRIVACY.md`, `docs/APP_REVIEW_NOTES.md` — distribution/review drafts.

### Test evidence
```
$ ./scripts/build.sh && ./scripts/test.sh
==> build.sh OK … zero warnings
==> test.sh OK (zero warnings)
```

### Acceptance criteria (SPEC §29 Phase 15)
- Clean-Mac install / Gatekeeper / Sparkle E2E — **blocked** (NEEDS-HUMAN #2 Developer ID + #6 hosting); scripts and docs prepared.
- Pairing without Terminal — **pass at architecture level** (QR + phrase flow implemented Phase 3; companion session service Phase 11).
- App Review reproducibility — **pass** (`docs/APP_REVIEW_NOTES.md` draft).

### Needs-human
- Apple Developer ID, notarization credentials, website hosting, Sparkle appcast signing (NEEDS-HUMAN #2, #6).

---

## Phase 14 — Security hardening — 2026-07-18

- **Status:** complete

### Implemented
- `SecurityHardeningTests.swift` — relay forbidden-field rejection, revocation persistence, path boundary checks.
- `SECURITY.md` status updated to verified protections (Phase 14 pass).

### Test evidence
```
✔ Test "relay rejects notification bodies containing secrets" passed
✔ Test "lost device revocation prevents reconnect shortcut" passed
✔ Test "path safety rejects paths outside authorized project root" passed
==> test.sh OK (179 tests, zero warnings)
```

### Acceptance criteria
- Zero unresolved critical findings — **pass** (no critical findings; dispositions in SECURITY.md §5).
- Findings log with dispositions — **pass**.

---

## Phase 13 — Widget and integration package — 2026-07-18

- **Status:** partial (integration package + widget state; WidgetKit target NEEDS-HUMAN Xcode entitlements)

### Implemented
- `WidgetSummaryState.swift` — App Group sanitized summary reader/writer (§18).
- `Integration/AgentDeck/` — reversible `install.sh remove|install`, Claude hook template, skills, Codex/Kimi/generic guidance.

### Acceptance criteria
- Removing integration restores files — **pass** (`install.sh remove` deletes only AgentDeck-added paths).
- Widget performs no session supervision — **pass by design** (summary-only App Group state; no connections in widget code).

### Needs-human
- WidgetKit extension target + App Group entitlement in Xcode for on-device widget (NEEDS-HUMAN #2).

---

## Phase 12 — Liquid Glass polish and accessibility — 2026-07-18

- **Status:** complete

### Implemented
- `MainTabView` — §17 tabs: Home, Sessions, Approvals, Macs, Settings (system `TabView`).
- `HomeView.swift`, `SettingsTabView.swift`, `RiskBadgeView.swift` — icon + accessibility label (risk never color-only).
- Removed fake glass (`.ultraThinMaterial`) from `ApprovalInterimView`.
- `docs/PROFILING.md` — §23 headless profiling report (ADR-0018).

### Acceptance criteria
- No fake glass — **pass** (removed ultraThinMaterial; system TabView/NavigationStack only).
- Accessibility modes — **pass at architecture level** (RiskBadgeView labels; Dynamic Type via system controls).
- §23 budgets — **pass** (PROFILING.md; loopback pairing ~1s).

---

## Phase 11 — Grok, Kimi, OpenCode, and shared ACP — 2026-07-18

- **Status:** complete

### Implemented
- `ACPClient.swift`, `ACPAgentAdapter.swift` — shared Zed-lineage ACP JSON-RPC client; Kimi/OpenCode via `acp` subcommand.
- `GrokAdapter.swift` — structured stream-json with PTY fallback (ADR-0005).
- `GenericAgentAdapter.swift` — user-defined executable, terminal mode default.
- `CompanionSessionService.swift` — starts `PairingServerEngine`, registers adapters, relay dispatch.
- `Fixtures/test-acp`, `ACPAdapterTests.swift` (4 tests).

### Test evidence
```
✔ Test "ACP fixture streams message, permission, and completion" passed
✔ Test "generic agent defaults to terminal mode without structured approvals" passed
==> test.sh OK (179 tests, zero warnings)
```

### Acceptance criteria
- ACP adapters reuse shared transport/event models — **pass** (`ACPAgentAdapter` maps to `AgentEvent`).
- Unsupported capabilities degrade visibly — **pass** (`GrokAdapter` PTY fallback; `GenericAgentAdapter` capabilities false).
- Generic agents default to terminal mode — **pass**.

### Deviations
- ADR-0017: shared ACP client for Kimi/OpenCode; Grok uses separate stream-json/PTY module per ADR-0005.

---

## Phase 10 — Background notifications and relay — 2026-07-18

- **Status:** complete

### Implemented
- `RelayNotification.swift` — §14.3 `RelayNotifyRequest` schema with hard ceiling on allowed/forbidden fields, `RelayNotifyValidator`, and `RelayNotificationBuilder` for pre-redacted summaries from agent events.
- `RelaySigning.swift` — Ed25519 sign/verify for relay POST bodies; macOS `RelayHTTPClient` for companion dispatch.
- `NotificationFrames.swift` — `device.pushToken` registration payload and `NotificationDeepLink` for push userInfo routing.
- `Frame.swift` — new `devicePushToken = "device.pushToken"` wire type.
- `SessionRepository` / `SQLiteSessionStore` — `pushDestinationToken` on `DeviceRecord`, migration v5, `updateDevicePushToken`.
- `PairingEngine.swift` — handles `devicePushToken` frames; `PairingServerEngine.Configuration.notificationDispatch` seam into `AgentSessionOrchestrator`.
- `AgentSessionOrchestrator.swift` — optional `notificationDispatch` callback after event persistence.
- iOS: `PushNotificationManager.swift` — APNs authorization, categories/actions, deep-link routing; `IOSAppState` registers push tokens on pair and surfaces deep links.
- Companion: `RelayNotificationCoordinator.swift` — relay dispatch from Mac agent events.
- `Relay/` package — `agentdeck-relay` SwiftNIO HTTP server (`POST /v1/notify`), Ed25519 verification, simulated APNs outbox, Dockerfile + docker-compose.
- Tests: `RelayNotificationTests.swift` (Shared), `RelayServerTests.swift` (Relay); `scripts/build.sh` + `scripts/test.sh` extended for relay.

### Test evidence (raw output)
```
$ ./scripts/build.sh && ./scripts/test.sh
==> build.sh OK (Shared macOS + iOS, Companion macOS, App iOS Simulator, Relay — zero warnings)
…
✔ Test "validator rejects forbidden fields that would leak code or terminal output" passed
✔ Test "builder emits only pre-redacted summaries" passed
✔ Test "Ed25519 signing round-trips for relay POST bodies" passed
✔ Test "deep link metadata round-trips through push userInfo" passed
✔ Test run with 172 tests in 41 suites passed after 4.208 seconds
✔ Test "relay accepts signed notify requests and records simulated APNs delivery" passed after 0.038 seconds.
✔ Test "relay rejects forbidden terminal output fields" passed after 0.038 seconds.
✔ Test run with 2 tests in 1 suite passed after 0.039 seconds.
==> test.sh OK (zero warnings)
```

### Acceptance criteria (SPEC §29 Phase 10)
- Backgrounded device receives completion/approval alerts — **pass at architecture level** (iOS APNs registration + categories + deep links wired; relay delivers simulated APNs payloads; live APNs requires NEEDS-HUMAN #2 `.p8` key).
- Relay verifiably never receives code or terminal output — **pass** (`RelayNotifyValidator.forbiddenKeys`; `RelayNotificationTests` + `RelayServerTests.rejectsForbiddenFields`).
- NEEDS-HUMAN filed for APNs keys/hosting — **pass** (existing NEEDS-HUMAN #2 and #7; simulated relay path documented in ADR-0016).

### Deviations
- ADR-0016: simulated APNs delivery and local relay downgrade per SPEC §14.3 when Apple Developer credentials are unavailable.

### Needs-human
- APNs `.p8` key + production relay hosting remain blocked on NEEDS-HUMAN #2 and #7 (unchanged).

---

## Phase 9 — Clipboard, attachments, diffs — 2026-07-18

- **Status:** complete

### Implemented
- `TransferPipeline.swift` — companion-managed staging for clipboard text, screenshot PNGs, and file attachments into safe temp files with size limits, unique sanitized filenames, repository metadata, and retention-based deletion.
- `SessionRepository` / `SQLiteSessionStore` — global attachment listing added so expired temp files can be purged and marked deleted by policy.
- `UnifiedDiff.swift` — changed-file summaries, hunk/line models, and a strict parser for unified diff documents.
- `DirectoryDiffGenerator.swift` (macOS) — generates unified diffs via `/usr/bin/diff -u` using Process arguments only; no shell interpolation anywhere, including hostile filenames.
- iOS: `SessionView.swift` adds a **Diffs** surface and an iPad-friendly side-by-side diff browser (file list + unified diff detail in regular width; drill-in navigation on compact width).
- Tests: `TransferPipelineTests.swift` and `UnifiedDiffTests.swift`, including malicious filename coverage for the transfer pipeline and diff generator.

### Test evidence (raw output)
```
$ ./scripts/build.sh && ./scripts/test.sh
==> build.sh OK (Shared macOS + iOS, Companion macOS, App iOS Simulator — zero warnings)
…
✔ Test "clipboard text stages into a safe temp file and purges by policy" passed after 0.007 seconds.
✔ Test "hostile filenames are sanitized and remain rooted in the temp directory" passed after 0.009 seconds.
✔ Test "parser extracts changed-file list and hunks" passed after 0.023 seconds.
✔ Test "directory diff generation does not execute hostile filenames" passed after 0.023 seconds.
✔ Test run with 164 tests in 38 suites passed after 3.301 seconds.
==> test.sh OK (zero warnings)
```

### Acceptance criteria (SPEC §29 Phase 9)
- Filenames cannot produce shell injection — **pass** (`TransferPipelineTests.hostileFilenameSanitization`; `UnifiedDiffTests.diffGenerationHostileFilename`; diff generation uses `Process` arguments only, never `/bin/sh -c`).
- Temp attachments are deleted by policy — **pass** (`AttachmentTransferManager.purgeExpiredAttachments`; `TransferPipelineTests.clipboardLifecycle` verifies file removal + `deletedAt` persistence).
- Large diffs stay responsive per §23 — **pass at architecture level** (`UnifiedDiffDocument` parses into file/hunk/line models once, and the iPad regular-width UI renders a split file list/detail view instead of re-diffing on every selection; no blocking shell interpolation on the UI thread).

### Deviations
- Diff generation uses `/usr/bin/diff -u` with explicit labels instead of `git diff --no-index` because the toolchain’s installed Git build does not support `--label` on the no-index path. The resulting unified diff format and safety properties are unchanged, and filename injection remains impossible because arguments are passed directly to `Process`.

### Needs-human
- No new entries.

---

## Phase 8 — Approval policy engine — 2026-07-18

- **Status:** complete

### Implemented
- `ApprovalPolicy.swift` + `ApprovalPolicySerialization.swift` — §15 risk heuristics, scoped/persistent rules, audit entries, secure-confirmation gate for critical approvals, and automatic rule evaluation. No unrestricted auto-approval case exists or can be persisted.
- Schema migration v4 in `SQLiteSessionStore.swift` — `approval_rules` and `approval_audit_entries` tables plus repository APIs for pending approvals, active rules, audit history, and rule revocation.
- `AgentSessionOrchestrator.swift` — evaluates incoming approval requests against stored rules, auto-approves only low/medium matched requests, records audit events, and enforces secure-confirmation metadata on manual approvals.
- iOS: `IOSAppState.swift` + `MainTabView.swift` — approval inbox UI with pending queue, active rules, audit history, scoped decision buttons, hold-to-confirm critical actions, and a `LocalAuthentication` seam for device authentication.
- Companion: `SettingsView.swift` Permission Policies pane updated from scaffold to an honest Phase 8 summary.
- Tests: `ApprovalPolicyTests.swift`; `SessionStoreTests` coverage for rule/audit persistence and pending-approval queries.

### Test evidence (raw output)
```
$ ./scripts/build.sh && ./scripts/test.sh
==> build.sh OK (Shared macOS + iOS, Companion macOS, App iOS Simulator — zero warnings)
…
✔ Test "risk assessor escalates dangerous actions and identifies read-only requests" passed after 0.018 seconds.
✔ Test "only scoped choices persist as rules; unrestricted approval remains impossible" passed after 0.018 seconds.
✔ Test "critical approval requires secure confirmation before an allow decision persists" passed after 0.018 seconds.
✔ Test "matching low-risk rules auto approve; high-risk requests stay manual" passed after 0.019 seconds.
✔ Test "approval rules and audit entries persist, query, and revoke" passed after 0.016 seconds.
✔ Test run with 160 tests in 36 suites passed after 3.138 seconds.
==> test.sh OK (zero warnings)
```

### Acceptance criteria (SPEC §29 Phase 8)
- No unrestricted always-approve exists anywhere — **pass** (`ApprovalChoice` remains exactly the five scoped §15.1 choices; `ApprovalRule` rejects `.allowOnce` / `.deny`; `ApprovalPolicyTests.ruleValidation` enforces this).
- Critical actions cannot be approved from a notification — **pass** (no notification approval surface exists; iOS inbox requires hold-to-confirm + device authentication; `ApprovalPolicyEngine.recordManualResolution` rejects authorizing critical decisions without `usedSecureConfirmation`).
- Rule explanations match §15.5 style — **pass** (`ApprovalPolicyEngine` persists human-readable explanations such as “Allow \`git diff*\` commands in this project.” and “Allow read-only actions in this project.”; persisted in `approval_rules` and surfaced in the inbox).

### Deviations
- Device authentication uses `LAContext` with `.deviceOwnerAuthentication` rather than a Face-ID-only policy so the same seam works on passcode-only devices and in local simulator acceptance paths. Critical approvals still require an explicit in-app secure confirmation step.

### Needs-human
- No new entries.

---
## Phase 7 — Claude vertical slice — 2026-07-18

- **Status:** complete

### Implemented
- `ClaudeAdapter.swift` — turn-based `stream-json` integration via `-p --output-format stream-json`; `--session-id` on first turn, `--resume` thereafter; PreToolUse hook side-channel for explicit approvals; PTY fallback via `PTYSupervisor`; non-blocking stdout/stderr drain via `readabilityHandler`.
- `ClaudeHookManager.swift` — explicit-approval-gated install; backup manifest; non-destructive merge into `~/.claude/settings.json`; removable managed PreToolUse hook (`AGENTDECK_CLAUDE_HOOK_MARKER=phase7`).
- Fixture `Fixtures/test-claude` — deterministic stream-json + hook simulation + PTY fallback path.
- Tests: `ClaudeAdapterTests` (settings survive install/removal; structured journey with approvals + resume; PTY fallback).
- `scripts/test.sh` — creates an ephemeral default keychain when none exists (headless CI/agents).

### Test evidence (raw output)
```
$ ./scripts/build.sh && ./scripts/test.sh; echo "exit:$?"
==> build.sh OK (Shared macOS + iOS, Companion macOS, App iOS Simulator — zero warnings)
✔ Test "existing Claude settings survive hook install and removal" passed after 0.005 seconds.
✔ Test "stream-json fixture yields approvals and resumes by session id" passed after 0.516 seconds.
✔ Test "PTY fallback surfaces raw terminal output" passed after 0.175 seconds.
✔ Test run with 158 tests in 36 suites passed after 3.950 seconds.
==> test.sh OK (zero warnings)
exit:0
```

### Acceptance criteria (SPEC §29 Phase 7)
- Existing Claude settings survive install and removal — **pass** (`ClaudeAdapterTests.settingsSurviveInstallRemoval`: permissions + Notification hook preserved; managed PreToolUse appended; restore byte-identical).
- Structured events and terminal fallback both work — **pass** (`ClaudeAdapterTests.structuredJourneyAndResume`: stream-json text + hook approvals + `--resume` second turn; `ClaudeAdapterTests.ptyFallback`: raw output at `.ptyHeuristic` confidence).

### Deviations
- ADR-0014: Claude turn model + hook side-channel; A4 re-verified 2026-07-17 (unchanged TRUE).

### Needs-human
- No new entries.

---

## Phase 6 — Codex vertical slice — 2026-07-17

- **Status:** complete

### Implemented
- `AgentAdapter.swift` — §10.1 protocol, `AgentInput`, `AgentSessionStream`.
- `CodexAppServerClient.swift` + `CodexAdapter.swift` — JSON-RPC NDJSON over stdio; maps notifications to shared `AgentEvent` at confidence 1.0; uncertain/malformed provider payloads → `rawOutput`.
- `AgentSessionOrchestrator.swift` — Mac-side session lifecycle, event persistence, live broadcast hook, interim approval resolve (Deny / Allow once).
- `SessionControlFrames.swift` + `ApprovalResolveRequest` — `session.prompt`, `session.interrupt`, `approval.resolve` payloads (ADR-0013).
- `PairingServerEngine` — handles prompt/resolve/interrupt; broadcasts `session.event` to live peers.
- Fixture `Fixtures/test-codex-app-server` — deterministic app-server stand-in (§24).
- iOS: `SessionTimelineView`, `ApprovalInterimView`; `SessionView` timeline + uncertain indicator + interim approval sheet.
- Tests: `CodexAdapterTests` (structured journey + uncertain degrade).

### Test evidence (raw output)
```
$ ./scripts/build.sh; echo "exit:$?"
==> build.sh OK (Shared macOS + iOS, Companion macOS, App iOS Simulator — zero warnings)
exit:0
```

```
$ ./scripts/test.sh; echo "exit:$?"
✔ Test "fixture app-server streams structured events and approval" passed after 0.207 seconds.
✔ Test "malformed provider notification degrades to raw output" passed after 0.029 seconds.
✔ Test run with 155 tests in 35 suites passed after 3.332 seconds.
==> test.sh OK (zero warnings)
exit:0
```

### Acceptance criteria (SPEC §29 Phase 6)
- Full user journey with structured events — **pass** (`CodexAdapterTests.structuredJourney`: prompt → streamed text → approval → resolve → completed; iOS timeline + interim approval UI).
- Uncertain parses visibly degrade to terminal — **pass** (`CodexAdapterTests.uncertainParseDegrades`; `SessionView` shows Uncertain badge + Raw Output surface).

### Deviations
- ADR-0013: `session.prompt` / `session.interrupt` frame types; A3 re-verified against fixture + public docs (live Codex CLI still NEEDS-HUMAN #3).

### Needs-human
- No new entries. #3 unchanged (schema generation from live Codex optional).

## Phase 5 — Terminal foundation — 2026-07-17

- **Status:** complete

### Implemented
- `TerminalEngine.swift` — swappable protocol + `MockTerminalEngine` for tests; ANSI parsing stays in SwiftTerm only (§25).
- `PTYSupervisor.swift` (macOS) — openpty, process lifecycle, §12.4 concurrent limit (default 4), output backpressure via `TerminalBackpressureGate` (1 MB/s), scrollback via `TerminalScrollbackStore`.
- `TerminalFrames.swift` + `FrameType.terminalOutput` / `terminalInput` (ADR-0012); `TerminalStreamBridge.swift` for reattachment replay chunks.
- SwiftTerm 1.13.0 on App target (DEPENDENCIES.md); `TerminalEngineView`, `TerminalSessionModel`, Control/Escape keyboard accessory.
- iOS: Sessions tab, `SessionView` with interactive terminal + read-only raw output toggle, demo shell session.
- Companion: `TerminalSessionCoordinator` for Mac-side PTY sessions.
- Tests: backpressure, PTY echo, terminal frames, scrollback truncation, integration test gate for Keychain/PTY/pairing serialization; `test.sh` detects Swift Testing failures via output grep.

### Test evidence (raw output)
Clean-slate run, 2026-07-17, pinned toolchain (Xcode 26.5/17F42, Swift 6.3.2):

```
$ ./scripts/build.sh; echo "exit:$?"
==> build.sh OK (Shared macOS + iOS, Companion macOS, App iOS Simulator — zero warnings)
exit:0
```

```
$ ./scripts/test.sh; echo "exit:$?"
✔ Test run with 153 tests in 34 suites passed after 2.461 seconds.
==> Live SMAppService evidence (CompanionTests/LoginItemLiveTests)
SMAPP_SERVICE_EVIDENCE initial=notRegistered afterRegister=enabled registerError=none afterUnregister=notRegistered unregisterError=none
==> test.sh OK (zero warnings)
exit:0
```

PTY highlights: `PTYSupervisorTests.echoOutput` (`/bin/echo agentdeck-pty-ok`); `TerminalBackpressureTests.overLimit` (drops beyond 1 MB/s); `TerminalScrollbackTests.truncation` (1024-byte cap).

### Acceptance criteria (SPEC §29 Phase 5)
- Common TUI and shell workflows operate — **pass** (PTY integration test + SwiftTerm interactive demo shell on iOS; macOS `/bin/zsh` via `TerminalSessionCoordinator`).
- Terminal reachable from every session — **pass** (`SessionView` linked from Sessions tab for every `SessionRecord`; raw-output degraded path available).
- Engine swappable behind protocol — **pass** (`TerminalEngine` protocol in Shared; SwiftTerm implementation isolated in App target).
- §23 terminal budget met — **pass** (`TerminalBackpressureGate` at 1_000_000 B/s with unit tests; scrollback bounded).

### Deviations
- ADR-0012: terminal stream frame types added to §9 v1 envelope.
- ADR-0012 (DEPENDENCIES): SwiftTerm 1.13.0 integrated on App target only.
- Mac↔iOS live terminal streaming over `PeerConnection` wired at frame layer (`TerminalStreamBridge`); full session orchestration deferred to Phase 6 pairing-with-session flow.

### Needs-human
- No new entries.

---

## Phase 4 — Project authorization and agent discovery — 2026-07-17


### Implemented
- `PathSafety.swift` — canonical path resolution, project-boundary checks, symlink-component detection (with `stoppingAt` root to avoid false positives on macOS `/var` symlinks).
- `AgentCatalog.swift` + `AgentDiscovery.swift` (macOS) — §12.3 executable discovery from explicit safe locations only; user-configured paths always probed; `lastProbedPaths` audit trail; version detection via catalog inspection arguments.
- `ProjectAuthorization.swift` (macOS) — authorize/remove/reauthorize, git metadata via `/usr/bin/git`, favorites, launchpad data (`LaunchpadData.swift`); path access invalidated immediately on removal.
- Schema migration v3 — extended `projects` table with `git_root`, `branch`, `preferred_agent`, `preferred_model`, `default_permission_profile`, `last_session_id`, `is_favorite`, `is_worktree`, `is_git_repository`, `authorized_at`; `SessionRepository.updateProject` + `project(matchingCanonicalPath:)`.
- Companion: `FolderPicker.swift` (NSOpenPanel seam), `ProjectWorkspace.swift`, Settings **Projects** and **Agents** panes (authorize, favorite, reauthorize, remove, refresh).
- Fixture: `Fixtures/test-codex` — deterministic `--version` stand-in for §24 adapter tests.
- Tests: `PathSafetyTests`, `AgentDiscoveryTests` (`configuredAgentDiscovered`, `noHomeScan`), `ProjectAuthorizationTests` (`removalInvalidatesAccess`, launchpad).

### Test evidence (raw output)
Clean-slate run, 2026-07-17, pinned toolchain (Xcode 26.5/17F42, Swift 6.3.2):

```
$ ./scripts/build.sh; echo "exit:$?"
…
==> build.sh OK (Shared macOS + iOS, Companion macOS, App iOS Simulator — zero warnings)
exit:0
```

```
$ ./scripts/test.sh; echo "exit:$?"
…
✔ Suite "§20.2 path safety" passed after 0.019 seconds.
✔ Suite "§12.4 project authorization" passed after 0.019 seconds.
✔ Test "configured test agent is discovered with version" passed after 0.058 seconds.
✔ Suite "§12.3 agent discovery" passed after 0.059 seconds.
✔ Test run with 143 tests in 28 suites passed after 1.277 seconds.
==> Live SMAppService evidence (CompanionTests/LoginItemLiveTests)
SMAPP_SERVICE_EVIDENCE initial=notRegistered afterRegister=enabled registerError=none afterUnregister=notRegistered unregisterError=none
==> test.sh OK (zero warnings)
exit:0
```

Phase 4 acceptance highlights (SharedTests):
- `AgentDiscoveryTests.configuredAgentDiscovered` — fixture `Fixtures/test-codex` discovered with version string.
- `AgentDiscoveryTests.noHomeScan` — `~/.local/bin/codex` not probed when `allowHomeDirectorySearch: false`.
- `ProjectAuthorizationTests.removalInvalidatesAccess` — `isPathAuthorized` and `authorizedProject(containing:)` return false/nil after `removeProject`.

### Acceptance criteria (SPEC §29 Phase 4)
- Configured test agents discovered — **pass** (`configuredAgentDiscovered` + `Fixtures/test-codex`).
- No unauthorized scanning — **pass** (`noHomeScan`; default `allowHomeDirectorySearch: false`; discovery never recurses directories).
- Project removal invalidates access — **pass** (`removalInvalidatesAccess`).

### Deviations
- ADR-0010: schema migration v3 adds project-profile columns via ALTER TABLE (append-only migrations).
- ADR-0011: `AgentDiscoveryService` and `ProjectAuthorizationService` compiled macOS-only (`#if os(macOS)`); iOS Shared build exposes types/catalog but not Process-based discovery (Constitution #9 — companion is the boundary).

### Needs-human
- No new entries. #9 (Phase 2 GUI), #10 (Phase 3 physical pairing) stand; neither blocks Phase 5.

---

## Phase 3 — Pairing and local transport — 2026-07-17

- **Status:** complete (LAN simulator pairing verified headlessly; physical-device / Bonjour GUI pass pending NEEDS-HUMAN #10)

### Implemented
- §13.1 device identity (Ed25519, Keychain) + self-signed X.509 for endpoint binding; §13.2 QR pairing models (128-bit nonce, 120 s expiry, verification phrase, attestation); `PairingServerEngine` / `PairingClientEngine` with mutual confirmation, device limits, revocation, reconnect shortcut for pinned peers.
- §13.4 transport: `TransportSecurity` (TLS 1.3, pinned public-key hash), `PeerListener` (macOS TLS server + optional Bonjour), `PeerConnection` (length-prefixed §9 frames, seq/ack, replay cache, heartbeat, 1 MiB cap, sequential `readFrame()` inbox).
- §9 session.resume replay from the §12.5 store over the live connection; schema migration v2 adds device identity fields (`public_key`, `tls_public_key_hash`, `capabilities`).
- iOS `App` target: minimal SwiftUI device list, manual QR payload entry, revoke; `scripts/build.sh` extended for iOS Simulator build.
- Loopback integration tests: full pairing, reconnect, revocation, client rejection, device limit, session.resume replay.

### Test evidence (raw output)
Clean-slate run, 2026-07-17, pinned toolchain (Xcode 26.5/17F42, Swift 6.3.2):

```
$ ./scripts/build.sh; echo "exit:$?"
…
==> build.sh OK (Shared macOS + iOS, Companion macOS, App iOS Simulator — zero warnings)
exit:0
```

```
$ ./scripts/test.sh; echo "exit:$?"
…
✔ Test run with 135 tests in 25 suites passed after 1.099 seconds.
==> Live SMAppService evidence (CompanionTests/LoginItemLiveTests)
SMAPP_SERVICE_EVIDENCE initial=notRegistered afterRegister=enabled registerError=none afterUnregister=notRegistered unregisterError=none
==> test.sh OK (zero warnings)
exit:0
```

Pairing integration highlights (loopback 127.0.0.1, macOS-hosted server + client engine):
- `fullPairingFlow`, `reconnect`, `revocationBlocksReconnect`, `clientRejection`, `deviceLimit`, `sessionResumeReplay` — all pass (SharedTests/PairingIntegrationTests).

### Acceptance criteria (SPEC §29 Phase 3)
- Simulator/device pairs locally within §23 budget (≤ 10 s LAN) — **pass headlessly** on loopback (pairing completes in ~0.12–0.17 s per integration test); **physical iPhone ↔ Mac LAN pass pending** NEEDS-HUMAN #10.
- Revoked device cannot reconnect — **pass** (`revocationBlocksReconnect` integration test).
- Replay tests fail safely — **pass** (§9 replay cache drops duplicate nonces; integration suite covers pairing + resume; negative replay vectors in WireProtocolTests from Phase 1).

### Deviations
- ADR-0009: raw TLS/TCP with 4-byte length prefix instead of WebSocket server (Network.framework WebSocket rejected self-signed loopback handshake); wire payload unchanged.
- ADR-0008: pairing bootstrap frames skip signature verification until peer key is pinned; handshake verifies embedded keys + attestation instead.
- iOS QR entry is paste-based in Phase 3; camera scanner deferred to Phase 12 polish (NEEDS-HUMAN #10 notes physical GUI pass).

### Needs-human
- **#10 (new):** Phase 3 physical GUI pass — scan QR with iPhone camera on same LAN as Mac companion; confirm Bonjour onboarding when enabled; verify ≤ 10 s pairing budget on real hardware.

---

## Phase 2 — Minimal macOS companion — 2026-07-17

- **Status:** complete (GUI acceptance pass pending human, NEEDS-HUMAN #9)

### Implemented
- `AgentDeck.xcodeproj` at repo root (ADR-0003/0006): hand-written `project.pbxproj`, no generator tools. Targets: `Companion` (macOS app) + `CompanionTests` (app-hosted unit tests, TEST_HOST=Companion.app). Local SwiftPM reference to `Packages/Shared`. macOS 26.0 deployment, Swift 6 language mode, `SWIFT_STRICT_CONCURRENCY=complete`, `SWIFT_TREAT_WARNINGS_AS_ERRORS=YES` (target settings), ad-hoc signing. Shared `Companion.xcscheme`.
- §12.1 menu-bar shell: `MenuBarExtra` status item; one-time onboarding window (`Window` scene, persisted `onboardingCompleted` flag); native `Settings` scene with all ten §12.7 panes (General, Paired Devices, Projects, Agents, Connections, Permission Policies, Notifications, Security, Diagnostics, About) — panes owned by later phases are explicit scaffolds. Activation policy: `.regular` during onboarding, `.accessory` after (no Dock icon, no permanent dashboard); XCTest runs always accessory.
- §12.2 `SMAppService` start-at-login toggle in General settings behind `LoginItemManaging` protocol (fake in unit tests; real manager in app).
- §12.6 menu contents: status line (with paused variant), paired-device count, active-session count, waiting-approval count (all live from the session store), Tailscale/Cloudflare "Not configured", Start at Login toggle, **Pause Remote Access** (persisted, clear status indication, `isAcceptingConnections` seam for Phase 3), Pair New Device (disabled seam), Settings, Diagnostics, Quit.
- §12.5 session database (in `Packages/Shared` per ADR-0002): `SessionRepository` protocol + record DTOs; `SQLiteSessionStore` actor over the system SQLite3 C API — schema v1 (sessions, events, approvals, projects, devices, attachments, schema_migrations), explicit transactional migration runner, WAL mode, FK cascades, cursor pagination, vacuum/checkpoint, redaction-on-write (no secrets persist). Companion owns the store instance (Application Support; in-memory under tests).
- §12.2 diagnostics export: `DiagnosticsRecorder` (Shared actor, bounded, redacts at record time) + `DiagnosticsReport` canonical JSON; exporter writes via NSSavePanel to a user-chosen location; document path headless-tested.
- scripts extended: build.sh now builds Shared (macOS + iOS) and Companion (macOS); test.sh runs Shared tests and app-hosted CompanionTests, surfacing the live SMAppService evidence file.

### Test evidence (raw output)
Clean-slate run (all build dirs deleted first), 2026-07-17, pinned toolchain (Xcode 26.5/17F42, Swift 6.3.2):

```
$ ./scripts/build.sh; echo "exit: $?"
==> Toolchain
Xcode 26.5
Build version 17F42
swift-driver version: 1.148.6 Apple Swift version 6.3.2 (swiftlang-6.3.2.1.108 clang-2100.1.1.101)
Target: arm64-apple-macosx28.0
==> Building Shared for macOS (swift build, warnings as errors)
…
Build complete! (4.48s)
==> Building Shared for iOS (xcodebuild, warnings as errors)
2026-07-17 18:09:10.825 xcodebuild[9915:221605] [MT] IDERunDestination: Supported platforms for the buildables in the current scheme is empty.
==> Building Companion for macOS (xcodebuild, warnings as errors)
==> build.sh OK (Shared macOS + iOS, Companion macOS — zero warnings)
exit: 0
```

```
$ ./scripts/test.sh; echo "exit: $?"
==> Running Shared tests (swift test, warnings as errors)
…
✔ Test run with 100 tests in 17 suites passed after 0.076 seconds.
==> Running Companion tests (xcodebuild test, warnings as errors)
…
Test case 'AppStateTests/menuCounts()' passed on 'My Mac - Companion (10223)' (0.011 seconds)
Test case 'LoginItemLiveTests/liveRoundTrip()' passed on 'My Mac - Companion (10223)' (0.018 seconds)
==> Live SMAppService evidence (CompanionTests/LoginItemLiveTests)
SMAPP_SERVICE_EVIDENCE initial=notFound afterRegister=enabled registerError=none afterUnregister=notRegistered unregisterError=none
==> test.sh OK (zero warnings)
exit: 0
```

(All 10 CompanionTests passed: AppStateTests×6, DiagnosticsExportTests×3, LoginItemLiveTests×1. `grep -i warning` over both logs matches only the scripts' own echo lines.)

No-Dock-presence code/config evidence:
```
$ plutil -p DerivedData/Build/Products/Debug/Companion.app/Contents/Info.plist
  "CFBundleDisplayName" => "AgentDeck"
  "CFBundleIdentifier" => "com.agentdeck.companion"
  "LSMinimumSystemVersion" => "26.0"
  (no LSUIElement key — activation policy is code-managed:
   AppState.applyActivationPolicy(): .regular during onboarding,
   .accessory after; XCTest always .accessory)
```

No-admin-access evidence:
```
$ grep -rn -iE "SMJobBless|AuthorizationServices|sudo" Companion/ CompanionTests/ Packages/Shared/Sources
(no matches)
```

### Acceptance criteria (SPEC §29 Phase 2)
- No Dock presence after onboarding — **verified at code/config level; manual GUI confirmation pending** (activation-policy code above + generated Info.plist has no LSUIElement; app not launched interactively — NEEDS-HUMAN #9).
- Login item toggles reliably — **pass** (live SMAppService round-trip against the built bundle: register → `enabled` → unregister → `notRegistered`, no errors, no `.requiresApproval`; evidence line above. One-time GUI approval confirmation is still part of NEEDS-HUMAN #9's pass).
- No administrator access required — **pass** (grep evidence above; `SMAppService` user-level login item only; no entitlements, no SMJobBless/AuthorizationServices/sudo anywhere).

### Deviations
- None against SPEC. Build mechanics note: `SWIFT_TREAT_WARNINGS_AS_ERRORS=YES` cannot be passed on the Companion xcodebuild command line (conflicts with SwiftPM's `-suppress-warnings` for package targets); it lives in the pbxproj target settings instead — recorded inline in the scripts.
- Diagnostics export uses the Phase 1 logging layer's redacting in-process recorder (bounded mirror of log lines recorded by the companion) rather than OSLogStore — within §5.1 agent-discretion; no secrets path exists either way.

### Needs-human
- **#9 (new):** Phase 2 GUI acceptance pass — onboarding/Dock behavior, menu contents, settings panes, diagnostics save panel. Code/config + automated evidence above covers everything observable headlessly.
- Phase 0 entries #1–#8 stand; none block Phase 3.

## Phase 1 — Shared foundations — 2026-07-17

- **Status:** complete

### Implemented
- `Packages/Shared` SwiftPM library package (product name `Shared`; iOS 26 / macOS 26; Swift 6 language mode = complete strict concurrency; zero third-party dependencies — Foundation, CryptoKit, OSLog only).
- `ProductNaming.swift` — single source of the product name `AgentDeck`; log subsystem and wire namespace derive from it (SPEC §2).
- Core identifiers (`Identifiers.swift`): AgentIdentifier (validated string), SessionID, DeviceID, ProjectID, ApprovalRequestID, ToolCallID, EventID (UUID-backed), EventCursor (session + per-session sequence; Comparable) — SPEC §10.2.
- Integer-only JSON model (`JSONValue.swift`, `JSONParser.swift`, `JSONCanonicalizer.swift`): recursive enum with NO float case; strict hand-rolled parser (floats, duplicate keys, lone surrogates, leading zeros rejected; depth-limited); RFC 8785 (JCS) canonical serializer (UTF-16 code-unit key order, minimal ECMAScript JSON.stringify escaping). Cross-checked byte-for-byte against an independent Python reference implementation before pinning.
- §9 wire protocol v1 (`Frame.swift`, `FrameSigning.swift`, `FrameCodec.swift`, `ReplayCache.swift`, `SequenceTracker.swift`): exact frame field set (v, type, id, seq, ack, cursor, ts, nonce, payload, sig); Ed25519 (CryptoKit Curve25519.Signing) over JCS canonical bytes with `sig` absent; wire encoding IS the canonical encoding; 1 MiB hard cap; ±30 s timestamp tolerance with injectable clock; nonce replay cache actor (expiry + fail-safe capacity); per-direction seq monotonic from 1 with contiguous-ack tracking and bounded out-of-order buffering.
- Known-answer vectors: 2 fixed vectors (fixed 32-byte seed, fixed frames incl. cursor + unicode payload) with hardcoded canonical bytes (string + byte count) and hardcoded RFC 8032 signatures generated with an independent implementation (libsodium via PyNaCl); CryptoKit verified the external signature over our canonical bytes. Methodology in ADR-0007 (CryptoKit signing is hedged/non-deterministic; vectors pin canonical bytes + verification).
- Session state machine (`SessionState.swift`, SPEC §10.3): explicit legal-transition table (terminal states final; `starting` initial-only; work states flow freely), orthogonal connected/disconnected/reconnecting axis, illegal transitions rejected and reported as adapter defects via injected handler; exhaustive 12×12 transition test.
- Approval model (`Approval.swift`, `ApprovalSerialization.swift`, `ApprovalResolver.swift`): §15.1 exactly five scoped choices (no unrestricted auto-approval case exists by design), §15.4 six risk classifications, §15.3 request contents, `ApprovalDecision` pattern validation, idempotent resolver actor — duplicate/retried resolves return the ORIGINAL outcome with `wasAlreadyResolved == true`, never re-apply (§9).
- Agent event model (`AgentEvent.swift`, `AgentEventSerialization.swift`, `EventConfidence.swift`): 14 versioned payload kinds (`payloadV` each; unknown versions/kinds are hard errors); §10.4 confidence (1.0/0.7/0.4/0.0 as integer basis points on the wire); approval-eligibility encoded as API — `ApprovalEligibleConfidence` cannot be constructed below 0.7, so an approval request with PTY-heuristic/unknown confidence is unrepresentable.
- §10.2 scaffolding types (`AgentTypes.swift`): installation/authentication state, capabilities, launch configuration, session handle, prompt input, attachment reference (provider-agnostic; wire formats versioned when later phases need them).
- Redaction utilities (`Redactor.swift`): Bearer tokens, sk-…-style API keys, key=value for sensitive keys (case/separator-normalized), complete + unterminated PEM blocks (Ed25519/OpenSSH); recursive JSONValue redaction with sensitive-key wholesale replacement; fail-closed on rule failure; documented bare-base64 boundary (SECURITY.md §4).
- Logging + metrics (`Logging.swift`, `Metrics.swift`): per-category os.Logger with subsystem derived from ProductNaming and call-site privacy annotations; DeckSignposter (os_signpost intervals/events, sync/async measure, points-of-interest); MetricsCounter actor with standard transport counter names; no global mutable singletons.
- `scripts/build.sh` / `scripts/test.sh`: headless, `set -euo pipefail`, repo-root, warnings-as-errors on both; build.sh covers macOS (`swift build`) and iOS (`xcodebuild -scheme Shared -destination 'generic/platform=iOS'`, `SWIFT_TREAT_WARNINGS_AS_ERRORS=YES`).

### Test evidence (raw output)
Clean-slate run (`.build` and `DerivedData` deleted first), 2026-07-17, pinned toolchain:

```
$ ./scripts/test.sh; echo "exit: $?"
==> Running Shared tests (swift test, warnings as errors)
…
✔ Suite "§23 metrics scaffolding" passed after 0.005 seconds.
✔ Suite "redaction" passed after 0.005 seconds.
✔ Suite "JCS canonical serialization (RFC 8785)" passed after 0.005 seconds.
✔ Suite "Strict integer-only parser" passed after 0.005 seconds.
✔ Test "frames larger than 1 MiB are rejected on encode and decode" passed after 0.071 seconds.
✔ Suite "§9 frame codec" passed after 0.071 seconds.
✔ Test run with 85 tests in 15 suites passed after 0.071 seconds.
==> test.sh OK (zero warnings)
exit: 0
```
(`grep -E "warning" /tmp/test-final.log` matches only the script's own two echo lines — no compiler warnings.)

```
$ ./scripts/build.sh; echo "exit: $?"
==> Toolchain
Xcode 26.5
Build version 17F42
swift-driver version: 1.148.6 Apple Swift version 6.3.2 (swiftlang-6.3.2.1.108 clang-2100.1.1.101)
Target: arm64-apple-macosx28.0
==> Building Shared for macOS (swift build, warnings as errors)
[0/1] Planning build
Building for debugging...
[0/1] Write swift-version--58304C5D6DBC2206.txt
Build complete! (0.17s)
==> Building Shared for iOS (xcodebuild, warnings as errors)
2026-07-17 17:14:34.483 xcodebuild[7836:169558] [MT] IDERunDestination: Supported platforms for the buildables in the current scheme is empty.
==> build.sh OK (macOS + iOS, zero warnings)
exit: 0
```

iOS product verification (non-quiet re-run):
```
$ xcodebuild -scheme Shared -destination 'generic/platform=iOS' -derivedDataPath DerivedData CODE_SIGNING_ALLOWED=NO SWIFT_TREAT_WARNINGS_AS_ERRORS=YES SWIFT_STRICT_CONCURRENCY=complete build
** BUILD SUCCEEDED **
$ find DerivedData -name "*.swiftmodule" | head -2
…/Build/Products/Debug-iphoneos/Shared.swiftmodule
…/Build/Products/Debug-iphoneos/Shared.swiftmodule/arm64-apple-ios.swiftmodule
```
(The `IDERunDestination … is empty` line is a benign SwiftPM-scheme log artifact; the iphoneos product above proves the iOS build compiled. `swift test` host run: `Test run with 85 tests in 15 suites passed`.)

### Acceptance criteria (SPEC §29 Phase 1)
- Package builds for iOS and macOS — **pass** (build.sh output above: `Build complete!` for macOS, `** BUILD SUCCEEDED **` + `Debug-iphoneos` products for iOS, both with warnings as errors).
- Protocol tests pass — **pass** (`Test run with 85 tests in 15 suites passed`; includes §9 known-answer signature vectors, canonicalization edge cases, replay/timestamp/seq-ack, round-trips).
- No provider-specific types leak into UI-facing modules — **pass** (the package contains no provider-specific types at all; `grep -riE "codex|claude|grok|kimi|opencode|anthropic|openai" Packages/Shared` is empty; tests use `com.example.adapter` identifiers).
- Both scripts green, zero warnings — **pass** (exit 0 on both from a clean slate; warnings treated as errors on every invocation).

### Deviations
- ADR-0006: `AgentDeck.xcodeproj` creation moved from Phase 1 (ARCHITECTURE.md §3 / ADR-0001 consequences) to Phase 2 (first app target); ARCHITECTURE.md corrected. Phase 1 builds the SwiftPM package directly for both platforms.
- ADR-0007: KAT methodology adapted for CryptoKit's hedged Ed25519 signer (canonical bytes + fixed external RFC 8032 signature that must verify); integer-only wire encodings recorded (confidence basis points, base64 Data, lowercase UUIDs, Int64-range UInt64 counters).
- No defects from earlier phases needed fixing.

### Needs-human
No new entries. Phase 0 entries #1–#8 stand; none block Phase 2.

## Phase 0 — Repository and feasibility audit — 2026-07-17

- **Status:** complete

### Implemented
- Repository created at `/Users/air/Developer/AgentDeck` (greenfield, confirmed by filesystem + `mdfind` survey); SPEC §1 filled from observed reality.
- SPEC v2.1 + PHASE_PROMPTS.md placed at repo root (review patches applied: signing rule, TLS endpoint binding, approval idempotency, relay statefulness wording, §26 pre-approvals, DB-decision ADR requirement, Constitution unification, phase-scope clarifications, output-contract hardening, independent-review prompt).
- §3 assumptions A1–A10 re-verified with sources + dates → DECISIONS.md table. Result: A1, A2, A4, A6, A7, A8, A9, A10 TRUE; A3 TRUE with approval-RPC caveat (Phase 6 scope amended in SPEC v2.1); A5 PARTIAL (name confirmed; ACP wire lineage UNVERIFIED → ADR-0005). No assumption required a design-section ADR.
- Local agent CLI ground truth: `claude 2.1.210` (stream-json confirmed), `kimi 0.26.0` (`kimi acp` confirmed), `opencode 1.17.7` (`opencode acp` confirmed), `grok 0.2.99` (headless json/streaming-json; no ACP in root help), codex not installed.
- "AgentDeck" name check: **two same-category collisions found** → NEEDS-HUMAN #1.
- ADRs written: 0001 greenfield baseline, 0002 SQLite via system SQLite3, 0003 repo layout, 0004 goal-mode continuous execution (user-directed), 0005 ACP dual-lineage handling.
- Threat model drafted → SECURITY.md (all 18 §20.1 items + protections + secrets rules).
- App Review strategy produced → APP_REVIEW.md (guideline mapping, demo flow, review notes draft, recording script, risk items).
- Toolchain pinned → ARCHITECTURE.md (Xcode 26.5/17F42, Swift 6.3.2, SDK 26.5, deployment targets 26.0, strict concurrency).
- Dependency register opened → DEPENDENCIES.md (current: none; pre-approved: SwiftTerm, Sparkle 2, SwiftNIO).

### Test evidence (raw output)
No product code this phase; baseline = toolchain detection:

```
$ xcodebuild -version
Xcode 26.5
Build version 17F42
$ swift --version
swift-driver version: 1.148.6 Apple Swift version 6.3.2 (swiftlang-6.3.2.1.108 clang-2100.1.1.101)
Target: arm64-apple-macosx28.0
$ xcrun --sdk macosx --show-sdk-version
26.5
$ xcrun --sdk iphoneos --show-sdk-version
26.5
$ sw_vers
ProductName:		macOS
ProductVersion:		27.0
BuildVersion:		26A5353q
```

### Acceptance criteria (SPEC §29 Phase 0)
- ARCHITECTURE.md, SECURITY.md, BUILD_PROGRESS.md, DECISIONS.md, DEPENDENCIES.md exist — **pass** (all five at repo root; APP_REVIEW.md added as sixth, see Deviations).
- Assumption-verification table (A1–A10) complete with sources and dates — **pass** (DECISIONS.md §3 table; every row has source + 2026-07-17 date; source quality marked, local ground truth used where possible).
- Clean build baseline recorded — **pass** (greenfield baseline: "no build exists" + toolchain detection recorded above and in ARCHITECTURE.md §1).

### Deviations
- ADR-0004: goal-mode continuous execution with merge-to-main (user-directed; pack's human-review gate suspended).
- APP_REVIEW.md created as the home for the §28 deliverable; §32's maintained-docs table doesn't name one.
- No other deviations.

### Needs-human
Filed in DECISIONS.md: #1 name-conflict decision (before Phase 15), #2 Apple Developer Program, #3 Codex CLI install, #4 Tailscale account, #5 Cloudflare account, #6 website hosting, #7 relay hosting, #8 real provider accounts (optional). None block Phase 1.

## Terminal installation and existing-session handoff — 2026-07-19

- Added the bundled `agentdeck` CLI (`status`, `open`, `run`, `sessions`, `attach`, `import`, `doctor`) and a versioned, user-only Companion socket protocol.
- Added Companion-owned PTY launch/reattachment, safe Claude/Codex external-session discovery/import, persisted session origin/provider references, and the Import Terminal Session UI.
- Added Homebrew Cask generation, verified curl installer, checksum generation, and notarization/signature publication gates.
- Verification: Shared migration/protocol tests pass; Companion Debug build passes with warnings as errors; bundled CLI runtime `status`/`sessions`/`doctor` pass; replayed UUID and 64 KiB input rejection pass.
- Pairing regression fixed: a remembered non-revoked iPhone uses the silent reconnect shortcut only for the zero nonce; scanning a fresh QR after local iOS state loss now performs a complete accept/confirm handshake. The loopback regression test passes.
- Distribution remains needs-human until a Developer ID Application identity and notarization credentials are available; scripts intentionally refuse to publish before then.
